{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "sh -c 'if [ -z \"${SITE_ENV:-}\" ]; then case \"${VERCEL_ENV:-}\" in preview) export SITE_ENV=staging ;; development) export SITE_ENV=development ;; esac; fi; npm run build'"
}
